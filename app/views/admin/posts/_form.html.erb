<%= form_for [:admin, post] do |form| %>

  <div class="mb-4">
    <%= form.text_field :title, placeholder: "Title" %>
    <%= errors post, :title %>
  </div>

  <div class="mb-4">
    <%= form.text_field :summary, placeholder: "Summary" %>
    <%= errors post, :summary %>
  </div>

  <div class="mb-4">
    <%= form.text_area :body, placeholder: "Body" %>
    <%= errors post, :body %>
  </div>

  <div class="mb-4">
    <%= form.text_field :taggables, placeholder: "Tags" %>
    <%= errors post, :tags %>
  </div>

  <div class="my-4">
    <%= form.text_field :canonical_url, placeholder: "Canonical URL" %>
    <%= errors post, :canonical_url %>
  </div>

  <%= form.submit class: "button" %>
<% end %>
